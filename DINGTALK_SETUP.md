# 钉钉机器人配置指南

## 🚀 快速配置步骤

### 1. 在钉钉群中添加机器人

1. 进入钉钉群
2. 点击群设置（右上角三个点）
3. 选择"群管理" → "机器人管理"
4. 点击"添加机器人"
5. 选择"自定义"机器人
6. 输入机器人名称（如：BNHBot）
7. 上传机器人头像（可选）

### 2. 配置机器人权限

**重要：必须开启以下权限**
- ✅ **接收消息** - 这是接收群消息的关键权限
- ✅ **发送消息** - 机器人回复消息的权限
- 🔒 **@机器人** - 用户@机器人的权限

### 3. 配置安全设置

选择以下三种安全设置之一：

#### 方式1: 关键词（推荐）
- 设置关键词：`报名`
- 机器人只回复包含关键词的消息
- 最简单，适合测试

#### 方式2: 签名
- 设置签名密钥
- 在 `.env` 文件中配置 `DINGTALK_SECRET`
- 更安全，推荐生产环境使用

#### 方式3: IP白名单
- 设置允许的IP地址
- 限制只有特定服务器可以调用

### 4. 获取Webhook URL

配置完成后，你会得到一个Webhook URL，格式如下：
```
https://oapi.dingtalk.com/robot/send?access_token=YOUR_ACCESS_TOKEN
```

### 5. 配置环境变量

在 `.env` 文件中配置：
```bash
DINGTALK_WEBHOOK=https://oapi.dingtalk.com/robot/send?access_token=YOUR_ACCESS_TOKEN
DINGTALK_SECRET=YOUR_SECRET_KEY  # 如果设置了签名
DINGTALK_AT_ALL=false  # 启动时是否@所有人，true/false
```

## 🔍 测试机器人

### 1. 启动服务
```bash
cargo run
```

### 2. 检查日志
启动时应该看到：
```
🔧 检查钉钉机器人配置...
✅ 钉钉机器人配置检查成功
```

### 3. 在群中测试
- 输入：`报名`
- 机器人应该回复报名链接

## ❌ 常见问题

### 问题1: 机器人没有回复
**可能原因：**
- 没有开启"接收消息"权限
- 消息不包含关键词（如果设置了关键词）
- Webhook URL错误

**解决方法：**
1. 检查机器人权限设置
2. 确保消息包含关键词
3. 验证Webhook URL

### 问题2: 机器人配置检查失败
**可能原因：**
- 网络连接问题
- 签名验证失败
- 权限不足

**解决方法：**
1. 检查网络连接
2. 验证签名密钥
3. 确认机器人权限

### 问题3: 无法@机器人
**可能原因：**
- 没有开启"@机器人"权限
- 机器人不在群中

**解决方法：**
1. 开启"@机器人"权限
2. 重新添加机器人到群中

## 📱 消息格式示例

### 触发报名功能
```
@机器人 报名
```

### 机器人回复示例
```
🎯 您好！

📝 请点击以下链接进行报名：
🔗 http://localhost:3000/register

💡 报名说明：
• 支持多种报名类型
• 填写完成后自动提交审核
• 管理员会及时处理您的申请

❓ 如有问题，请联系管理员
```

## 🔧 高级配置

### 自定义关键词
可以在代码中修改关键词检测逻辑：
```rust
// 在 src/handlers/dingtalk_webhook.rs 中
if text_content.contains("报名") {
    // 处理报名请求
}
```

### 自定义回复消息
可以修改回复消息的内容和格式：
```rust
let reply_message = "您的自定义消息内容";
```

## 📞 技术支持

如果遇到问题，请检查：
1. 机器人权限设置
2. 环境变量配置
3. 网络连接状态
4. 服务日志输出
